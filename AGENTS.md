# Project Style

The code style in this project is primarily based on compactness.

- Do not introduce a variable when the value is used only once or immediately in place.
- Prefer inline functions and expressions where they keep the code readable.
- Create new functions only for code reuse.
- Prefer `if/then/else` blocks over early returns.
